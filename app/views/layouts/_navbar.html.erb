<!-- Navigation -->
<nav id="mainNav" class="navbar navbar-default navbar-custom navbar-fixed-top">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header page-scroll">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" style="padding:4px 7px; margin: 0 10px 8px 0;">
                <span class="sr-only">Toggle navigation</span><i class="fa fa-bars" style="color: white;"></i>
            </button>
            <%= link_to "Picture Gallery", root_url, class: "navbar-brand" %>
            </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <% unless (!signed_in?) || (signed_in? && current_user.role == "pending") %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Albums <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <% @galleries.each do |gallery| %>
                        <li><%= link_to "#{gallery.date.strftime('%Y')} #{gallery.name} (#{gallery.pictures.count})", gallery_path(gallery) %> </li> 
                    <% end %>
                </ul>
              </li>
            <% end %>

            <% if signed_in? && current_user.role == "admin" %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin<span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "Admin Panel", admin_panel_path %></li>
                    <li role="seperator" class="divider"></li>
                    <li><%= link_to "Galleries Panel", galleries_path %></li>
                    <li><%= link_to "Add Gallery", new_gallery_path %></li>
                  </ul>
                </li>
            <% end %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><% if signed_in? %> Profile<span class="caret"></span><% end %></a>
                <ul class="dropdown-menu">
                  <% if current_user %>
                    <li><%= link_to 'Edit Profile', edit_user_registration_path %></li>
                    <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
                  <% end %>
                </ul>
            </li>
          </ul>
        </div>
        <!-- /.navbar-collapse -->
    <!-- /.container-fluid -->
</nav>