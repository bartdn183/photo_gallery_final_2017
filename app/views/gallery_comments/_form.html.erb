<%= form_for(gallery_comment) do |f| %>
  <% if gallery_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gallery_comment.errors.count, "error") %> prohibited this gallery_comment from being saved:</h2>

      <ul>
      <% gallery_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group hidden">
    <%= f.label :gallery_id %>
    <%= f.number_field :gallery_id, value: @gallery_comment.gallery_id %>
  </div>

  <div class="form-group">
    <%= f.text_area :comment_entry, class: "form-control form-control-xl", placeholder: "Type comment...", rows: 4 %>
  </div>

  <div class="actions">
    <%= f.submit "UPDATE COMMENT", class: "btn btn-success btn-block btn-large" %>
  </div>
<% end %>

